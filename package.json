{
  "name": "pocket-to-pinboard",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "ts-node run-local.ts",
    "test": "jest --rootDir=src",
    "build": "tsc",
    "prebuild": "rm -rf dist .aws-sam",
    "postbuild": "./package.sh"
  },
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "@types/node": "^17.0.23",
    "@types/node-fetch": "^2.6.1",
    "jest": "^27.5.1",
    "ts-jest": "^27.1.4",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.358.0",
    "node-fetch": "^2.6.7"
  }
}
